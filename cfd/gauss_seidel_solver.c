#include "solver.h"

void gausee_seidel_solver(
    double **u,
    double **v,
    double **p,
    double DENSITY,
    double element_lengthint,
    double dt, int rows, int cols)
{

    for (int i = 0; i < rows; i++)
    {
        for (int j = 0; j < cols; j++)
        {
            //double d = 
        }
    }

}